|%
+$  install
  [local=term =ship =desk] 
--
|_  inst=install
++  grad  %noun
++  grow
  |%
  ++  noun  inst
  ++  json
    =,  enjs:format
    %-  pairs
    :~  local+(cord local.inst)
        desk+(cord desk.inst)
        ship+(ship ship.inst)
    ==
  --
++  grab
  |%
  ++  noun  install
  ++  json
    ^-  $-(^json install)
    =,  dejs:format
    %-  ot
    :~  local+so
        ship+(su ;~(pfix sig fed:ag))
        desk+so
    ==
  --
--
