|%
+$  install
  [local=term =ship =desk] 
--
|_  inst=install
++  grad  %noun
++  grow
  |%
  ++  noun  inst
  ++  json
    =,  enjs:format
    %-  pr
    :~  local+(co local.inst)
        desk+(co desk.inst)
        ship+(hp ship.inst)
    ==
  --
++  grab
  |%
  ++  noun  install
  ++  json
    ^-  $-(^json install)
    =,  dejs:format
    %-  ot
    :~  local+so
        ship+sp
        desk+so
    ==
  --
--
